#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    string s="";
    string temp;
    stack<char> st;
    int flag=0;
    int last;
    for(int i=0;i<n;i++)
    {
        char c;
        cin>>c;
        if(c=='(')
        {
            temp+=c;    
            
        }
        if(c==')')
        {
            if(flag)
            {
                s.substr();
            }
        }
    }
}